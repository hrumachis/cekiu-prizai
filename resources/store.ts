import Vue from 'vue'
import Vuex from "vuex"
import { RootState } from './models/common'

// Attach vuex to Vue
Vue.use( Vuex );

export default new Vuex.Store( {
    // ---> Initaited variables
    state: {
    },
    // ---> Getters
    getters: {
        // -> Valuables

        // -> States
    },
    // ---> Methods
    mutations: { 
        // -> Setters

        // -> Actions
        
    } 
} );